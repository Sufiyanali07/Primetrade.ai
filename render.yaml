# Render Blueprint – optional. Use Dashboard or this file.
# Docs: https://render.com/docs/blueprint-spec

services:
  - type: web
    name: primetrade-api
    runtime: python

    buildCommand: pip install -r requirements.txt
    # Render sets PORT (e.g. 10000). Must use 0.0.0.0 so the service is reachable.
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT

    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: primetrade-db
          property: connectionString
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        sync: false

databases:
  - name: primetrade-db
    databaseName: primetrade
    user: primetrade
